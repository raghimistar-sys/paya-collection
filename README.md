# Paya Collection Demo

Demo-ready Vite/React version of the uploaded collection application.

## Local run
1. Install Node.js 20+
2. Run `npm install`
3. Run `npm run dev`

## Production build
`npm run build`

The app uses browser localStorage for demo persistence.
This is suitable for a presentation/demo, not production customer data.
